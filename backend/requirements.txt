# Python Version Compatibility: 3.9 - 3.13 recommended
# Note: Python 3.14+ may have compatibility issues with some packages
# If you encounter installation errors, try Python 3.11 or 3.12
#
# Use pyenv to manage Python versions:
#   pyenv install 3.12
#   pyenv local 3.12
#
# Then recreate the virtual environment:
#   rm -rf venv
#   python -m venv venv
#   source venv/bin/activate
#   pip install --upgrade pip setuptools wheel
#   pip install -r requirements.txt

# Core FastAPI dependencies
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
python-multipart>=0.0.6,<1.0.0

# Database
sqlalchemy>=2.0.25,<3.0.0
alembic>=1.13.1,<2.0.0
psycopg2-binary>=2.9.9,<3.0.0

# Authentication & Security
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
bcrypt>=4.1.2,<5.0.0

# Data validation & settings
pydantic>=2.5.3,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
email-validator>=2.1.0,<3.0.0

# Third-party integrations
stripe>=7.11.0,<8.0.0
redis>=5.0.1,<6.0.0
celery>=5.3.6,<6.0.0
requests>=2.31.0,<3.0.0
boto3>=1.34.26,<2.0.0

# File handling - using flexible version for Python 3.14 compatibility
pillow>=10.3.0
aiofiles>=23.2.1,<24.0.0
google-auth>=2.27.0,<3.0.0
